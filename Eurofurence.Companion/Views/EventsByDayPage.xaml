<Page
    x:Class="Eurofurence.Companion.Views.EventsByDayPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Eurofurence.Companion.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    DataContext="{Binding Source={StaticResource ViewModelLocator}, Path=EventsViewModel}"
    >

    <Grid x:Name="LayoutRoot">

     


            <Grid x:Name="ContentRoot" Margin="19,9.5,19,0" DataContext="{Binding Path=EventEntries}">
                <ListView ItemsSource="{Binding}" x:Name="EventList" IsItemClickEnabled="True">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <Grid>
                                <interactivity:Interaction.Behaviors>
                                    <interactivity:BehaviorCollection>
                                        <core:EventTriggerBehavior EventName="Tapped">
                                            <core:InvokeCommandAction 
                                                Command="{Binding Source={StaticResource ViewModelLocator}, Path=NavigationViewModel.NavigateToEventDetailPage}"
                                                CommandParameter="{Binding}"
                                                ></core:InvokeCommandAction>
                                        </core:EventTriggerBehavior>
                                    </interactivity:BehaviorCollection>
                                </interactivity:Interaction.Behaviors>
                                
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="90"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <StackPanel Grid.Column="0">
                                    <TextBlock Text="{Binding StartTime, Converter={StaticResource StringFormatConverter}, ConverterParameter='\{0:hh\\:mm}'}" Style="{StaticResource TitleTextBlockStyle}"/>
                                    <TextBlock Text="{Binding EndTime, Converter={StaticResource StringFormatConverter}, ConverterParameter='until \{0:hh\\:mm}'}"
                                            Foreground="{ThemeResource PhoneMidBrush}"/>
                                    <!--<TextBlock Text="{Binding EndTime}"  Foreground="{ThemeResource PhoneMidBrush}"/>-->
                                </StackPanel>


                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Title}" Style="{StaticResource TitleTextBlockStyle}" TextWrapping="Wrap"></TextBlock>
                                    <TextBlock Text="{Binding ConferenceRoom.Name}"  Foreground="{ThemeResource PhoneMidBrush}" TextWrapping="Wrap">
                                    </TextBlock>
                                    <Image Source="{Binding Image.Content, Converter={StaticResource BytesToImageConverter}}"></Image>
                                    <!--<TextBlock Text="{Binding Abstract}" TextAlignment="Justify" TextWrapping="Wrap" Foreground="{ThemeResource PhoneMidBrush}"></TextBlock>-->
                                </StackPanel>
                            </Grid>

                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </Grid>


    </Grid>
</Page>
